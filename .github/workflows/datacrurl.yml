name: datacrurl
on:
  schedule:
   #- cron: "0 1 * * *"
   - cron: "4,14,24,34,44,54 * * * *"
  workflow_dispatch:
 
#xxx
jobs:
  build:
    runs-on: ubuntu-18.04 #ubuntu-latest
    steps:        
     - name: Download zip
       env:
          url: ${{ secrets.crurl }}
       run: |
          echo Download dll
          wget -q --output-document=zip2.zip $url 
          echo unzip..........
          unzip zip2.zip -d zip2
          sudo chmod -R +X zip2 
          cd zip2
          dotnet CrawlData.dll
